{
  "name": "project",
  "version": "0.1.0",
  "private": true,
  "devDependencies": {
    "@sap/approuter": "9.3.0",
    "@sap/html5-repo-mock": "2.0.1"
  },
  "scripts": {
    "build-businesspartners": "cd businesspartners && if [ ! -d ${workspaceFolder}/VueDemo/businesspartners/node_modules/@vue/cli-service ] && [ ! -d ${workspaceFolder}/VueDemo/node_modules/@vue/cli-service ]; then npm i; fi && npm run build"
  }
}
